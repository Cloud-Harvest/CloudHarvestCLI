# Harvest CLI Configuration
All YAML files within this folder will automatically be loaded into Harvest. The group name for each configuration file is defined by the base filename, less the `.yaml` extension.

